# What transcribe_final.py needs. Install with:  pip install -r requirements.txt
# (ffmpeg is a system tool, not a pip package: brew install ffmpeg)
mlx-whisper
torch
torchaudio
soundfile
pyannote.audio
python-docx
